let carro=[]
function adicionarDesconto(desconto){
    carro.push({desconto});
    console.log(´$desconto})
    console.log {(descontos)} sera de 10%{preco.toFixed(2)};
function adicionarDesconto(valorgasto)
carro.push({valorGasto})sera de 5%{preco.ToFixed(2)}[""]
